package main

import (
	"{{ .PackageName }}/pkg/lambda"
    "{{ .PackageName }}/{{ .ServicePath }}/{{ if not .IsLegacy }}cmd/{{ end }}plain/{{ .LambdaName }}/handler"
)

func main() {
    lambda.Start(handler.Bootstrap())
}