{
  "name": "{{ .ServiceName }}",
  "devDependencies": {
    "serverless": "^3.38.0",
    "serverless-hooks": "^1.0.0",
    "serverless-plugin-datadog": "^5.72.0",
    "serverless-plugin-go": "^0.0.3",
    "serverless-plugin-lambda-insights": "^2.0.0",
    "serverless-plugin-s3-remover": "^0.1.6",
    "serverless-plugin-pkl": "^0.1.0",
    "serverless-prune-plugin": "^2.1.0",
    "serverless-plugin-warmup": "^8.3.0",
    "serverless-domain-manager": "^7.4.0"
  },
  "scripts": {
    "hook:before:deploy:deploy": "./node_modules/.bin/sls create_domain --stage $STAGE --verbose $SLS_PARAMS",
    "deploy": "./node_modules/.bin/sls deploy --stage $STAGE --verbose $SLS_PARAMS",
    "print": "./node_modules/.bin/sls print --stage $STAGE --verbose $SLS_PARAMS",
    "deploy:function": "./node_modules/.bin/sls deploy function -f $FUNCTION_NAME --stage $STAGE --update-config --verbose $SLS_PARAMS"
  }
}